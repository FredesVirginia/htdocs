<!DOCTYPE html>
<html>
<head>
  <title>Editar Usuario</title>
  <style>
    /* Estilos CSS para el formulario de edición */
    body {
      font-family: Arial, sans-serif;
    }

    form {
      width: 50%;
      margin: 0 auto;
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    label {
      display: block;
      margin-bottom: 5px;
    }

    input[type="text"],
    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 10px;
      margin-bottom: 20px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    input[type="submit"] {
      background-color: #333;
      color: #fff;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <!-- Menú superior -->
  <div class="menu">
    <a href="/">Inicio</a>
    <!-- Agrega aquí más enlaces del menú si es necesario -->
  </div>

  <!-- Formulario de edición -->
  <form action="/api/Docentes/CuentasEstudiantes/editar/<%= usuario.id %>" method="POST">
    <label for="nombre">Nombre:</label>
    <input type="text" id="nombre" name="nombre" value="<%= usuario.nombre %>">
    <br>
    <label for="email">Correo:</label>
    <input type="email" id="email" name="email" value="<%= usuario.email %>">
    <br>
    <label for="password">Contraseña:</label>
    <input type="password" id="password" name="password" value="<%= usuario.password %>">
    <br>
    <input type="submit" value="Guardar">
  </form>
</body>
</html>
